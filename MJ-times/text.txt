try {
    // 소스코드를 쓴다
    // 이안에서 에러가 발생하면
}catch(error) {
    // catch가 에러를 잡아준다
}

// 에러핸들링 하고 싶으면 소스코드르 try안에 써라 
// 에러가 발생되는 순간 그 코드는 거기서 끝이남


// edge케이스 : 아직 핸들링 되지 않은 케이스 = 예상치 못한 케이스(에러)를 잡는게 중요하다.

// 페이지네이션
